{% extends 'base.html.twig' %}

{% block title %}Post{% endblock %}

{% block body %}
    <h1>Post</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>
            <tr>
                <th>Data</th>
                <td>{{ post.data ? post.data|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>
   
    <a href="{{ path('app_post_index') }}">back to list</a>

    <a href="{{ path('app_post_edit', {'id': post.id}) }}">editez</a>

    {{ include('post/_delete_form.html.twig') }}
    {{ include('comment/new.html.twig', {'id': post.id, 'form': form}) }}
{% endblock %}
